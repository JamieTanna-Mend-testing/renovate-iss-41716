[project]
name = "my-project"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pyyaml==6.0.3",
    "requests==2.32.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff==0.14.11",
    "mypy==1.19.1",
]
